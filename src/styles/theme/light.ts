import {Theme} from 'react-native-elements';

const theme: Theme = {
  colors: {
    primary: '#40B659',
  },
  Card: {
    containerStyle: {
      borderRadius: 12,
    },
    imageWrapperStyle: {
      borderRadius: 12,
      overflow: 'hidden',
    },
  },
};

export default theme;
